package interfaces;

/**
 * Interface implementada pela classe de TextField.
 * */
public interface ITextField {
	
	/**
	 * Verifica se o elemento está disposto na tela, após verificar insere um texto passado pelo desenvolvedor.
	 * */
	ITextField setText(String text);
}
